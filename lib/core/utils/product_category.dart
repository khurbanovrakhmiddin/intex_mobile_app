import 'package:easy_localization/easy_localization.dart';import 'package:flutter/material.dart';import 'package:flutter_bloc/flutter_bloc.dart';import 'package:intex_mobile_app/core/utils/custom_card.dart';import 'package:intex_mobile_app/features/screens/cart_page/bloc/products_bloc.dart';import 'package:lazy_load_scrollview/lazy_load_scrollview.dart';class ProductCategory extends StatelessWidget {  final String type;  final  onPageEnded;  final int page;  const ProductCategory({Key? key, required this.type, required this.page, this.onPageEnded}) : super(key: key);  @override  Widget build(BuildContext context) {    final bloc = context.read<ProductsBloc>();    return Column(      crossAxisAlignment: CrossAxisAlignment.start,      children: [        Padding(          padding: const EdgeInsets.symmetric(horizontal: 20),          child: Text(            type.tr().toString(),            style: Theme.of(context)                .textTheme                .headline2                ?.copyWith(fontSize: 18),          ),        ),        const SizedBox(height: 29,),        LazyLoadScrollView(          onEndOfPage: () => onPageEnded,          child: ListView.builder(            itemCount: bloc.first.length,            itemBuilder: (context, position) {              return  CustomCard(onPressed: () {},catalog: ,),;            },          ),        ),      ],    );  }}